// ==========================================================================
// Fichier: /public/js/app.js
// Description: Logique JavaScript Frontend - EduMali Suivi Pédagogique (Laravel Version - RESTful API)
// Version: FINALE Complète - Syntaxe Re-vérifiée
// Date: 2025-04-12
// ==========================================================================

document.addEventListener('DOMContentLoaded', () => {

    console.log("EduMali Suivi Pédagogique - JavaScript Initialisé (Laravel RESTful).");

    // ==========================================================
    // === DOM ÉLÉMENTS GLOBAUX ===
    // ==========================================================
    const globalAlertContainer = document.getElementById('global-alert-container');
    const navLinks = document.querySelectorAll('.nav-link');
    const contentSections = document.querySelectorAll('.content-section');
    // Enseignants
    const teacherForm = document.getElementById('teacher-form');
    const teacherList = document.getElementById('teacher-list');
    const teacherFormTitle = document.getElementById('teacher-form-title');
    const saveTeacherBtn = document.getElementById('save-teacher-btn');
    const cancelTeacherBtn = document.getElementById('cancel-teacher-btn');
    const teacherIdInput = document.getElementById('teacher-id');
    const teacherMatieresListContainer = document.getElementById('teacher-matieres-list');
    // Emplois du Temps
    const selectTeacherSchedule = document.getElementById('select-teacher-schedule');
    const teacherTimetableContainer = document.getElementById('teacher-timetable-container');
    const printTimetableButton = document.getElementById('print-timetable-btn');
    const resetTimetableButton = document.getElementById('reset-timetable-btn');
    // Structure
    const classesStructureContainer = document.getElementById('classes-structure-container');
    // Planning
    const planningForm = document.getElementById('planning-form');
    const planTeacherSelect = document.getElementById('plan-teacher');
    const planLessonSelect = document.getElementById('plan-lesson-select');
    const planLessonDetailsDiv = document.getElementById('plan-lesson-details');
    const planLessonDescInput = document.getElementById('plan-lesson-desc');
    const planSubmitButton = planningForm ? planningForm.querySelector('button[type="submit"]') : null;
    const viewPlanningDateInput = document.getElementById('view-planning-date');
    const studioPlanningView = document.getElementById('studio-planning-view');
    // Feuille de Route
    const selectClasseRoute = document.getElementById('select-classe-route');
    const generateRouteBtn = document.getElementById('generate-route-btn');
    const generateBlankBtn = document.getElementById('generate-blank-route-btn');
    const saveChangesBtn = document.getElementById('save-route-changes-btn');
    const printFdrBtn = document.getElementById('print-route-btn');
    const routeContent = document.getElementById('feuille-route-content');

    // ==========================================================
    // === VARIABLES GLOBALES & CONFIG ===
    // ==========================================================
    const statusOptions = [ { value: 'torecord', text: '🔜 À enregistrer' }, { value: 'editing', text: '🔄 En montage' }, { value: 'review', text: '🟡 À revoir' }, { value: 'validated', text: '✅ Validée' }, { value: 'redo', text: '❌ À refaire' }];
    let localTeacherCache = [];
    let allMatieresCache = [];
    let currentStructureCache = null;
    let currentTeacherSchedule = {};
    let saveTimeout = null;

    // ==========================================================
    // === FONCTIONS UTILITAIRES ===
    // ==========================================================
    function showAlert(message, type = 'info', container = globalAlertContainer) { if (!container) { console.warn("Conteneur d'alerte non trouvé"); return; } try { const alertDiv = document.createElement('div'); alertDiv.className = `alert-message alert-${type} p-4 mb-4 border rounded text-sm shadow`; switch (type) { case 'success': alertDiv.classList.add('bg-green-100', 'border-green-300', 'text-green-800'); break; case 'error': alertDiv.classList.add('bg-red-100', 'border-red-300', 'text-red-800'); break; case 'warning': alertDiv.classList.add('bg-yellow-100', 'border-yellow-300', 'text-yellow-800'); break; case 'info': default: alertDiv.classList.add('bg-sky-100', 'border-sky-300', 'text-sky-800'); } alertDiv.textContent = message; container.innerHTML = ''; container.appendChild(alertDiv); setTimeout(() => { if (alertDiv.parentNode === container && alertDiv.textContent === message) { container.innerHTML = ''; } }, 6000); } catch(e) { console.error("Erreur dans showAlert:", e); } }
    function clearGlobalAlert() { if (globalAlertContainer) globalAlertContainer.innerHTML = ''; }
    function escapeJsString(str) { if (str === null || typeof str === 'undefined') return ''; return String(str).replace(/'/g, "\\'").replace(/"/g, '\\"').replace(/`/g, "\\`"); }
    async function handleApiResponse(response) { const clone = response.clone(); try { const data = await response.json(); if (!response.ok) { let errorMsg = `Erreur ${response.status}`; if(data) { if (data.message) errorMsg = data.message; if (data.error) errorMsg = data.error; if (data.errors) { let validationErrors = Object.values(data.errors).map(errArray => errArray.join(', ')).join(' '); errorMsg = `Validation: ${validationErrors}`; } } throw new Error(errorMsg); } if (response.status === 204) { return { message: "Opération réussie." }; } return data; } catch (jsonError) { const text = await clone.text(); console.error("Réponse non-JSON:", text); if (!response.ok) { const titleMatch = text.match(/<title>(.*?)<\/title>/i); const errorTitle = titleMatch ? titleMatch[1] : `Erreur ${response.status}`; throw new Error(`Erreur Serveur: ${errorTitle}`); } else { throw new Error('Réponse serveur invalide.'); } } }
    function handleApiError(error) { console.error("Erreur API:", error); let displayMessage = "Une erreur est survenue."; if (error.message) { if(error.message.toLowerCase().includes('failed to fetch')) { displayMessage = "Erreur réseau. Vérifiez connexion/serveur."; } else if (error.message.startsWith('Erreur Serveur:') || error.message.startsWith('Erreur validation:') || error.message.startsWith('Erreur ') || error.message.startsWith('Non planifiable:') || error.message.includes('invalide')) { displayMessage = error.message; } else if (error.message.length < 150) { displayMessage = error.message; } else { displayMessage = "Une erreur technique est survenue."; } } showAlert(displayMessage, 'error'); }
    function printElement(elementToPrint, title = "Impression") { if (!elementToPrint) { console.error("Element à imprimer non trouvé"); return; } try { const printContents = elementToPrint.outerHTML; const logoPath = '/images/logo_edumali.png'; fetch(logoPath).then(res => { if (!res.ok) { console.warn('Logo non trouvé, impression sans logo.'); return null; } return res.blob(); }).then(blob => { const reader = new FileReader(); reader.onloadend = () => { const logoBase64 = reader.result; const primaryColor = '#faf0d6'; const secondaryColor = '#ffc107'; const accentColor = '#198754'; const textDark = '#2e2e2e'; const bgColor = '#ffffff'; const popupWin = window.open('', '_blank', 'top=0,left=0,height=100%,width=auto'); if (!popupWin) { showAlert("Vérifiez bloqueurs de popup.", "error"); return; } popupWin.document.open(); popupWin.document.write(`<!DOCTYPE html><html lang="fr"><head><meta charset="UTF-8"><title>${title}</title><style>@media print{body{-webkit-print-color-adjust:exact !important;print-color-adjust:exact !important;background-color:${bgColor} !important;}.print-header{border-bottom-color:${secondaryColor} !important;}th{background-color:${primaryColor} !important;color:${textDark} !important;}td.available{background-color:#e0f2fe !important;}td.prep{background-color:#fef9c3 !important;}td.record{background-color:#dcfce7 !important;}td.unavailable{background-color:#fecaca !important;}.no-print{display:none !important;}table,tr,td,th,tbody,thead,tfoot,li,span,p,strong,h1,h2,h3,h4,h5,div:not(.print-header){display:revert !important;visibility:visible !important;}}body{font-family:'Inter',sans-serif;padding:20px;font-size:10pt;line-height:1.4;color:${textDark};background-color:${bgColor};}.print-header{display:flex;justify-content:space-between;align-items:center;border-bottom:2px solid ${secondaryColor};padding-bottom:10px;margin-bottom:20px;}.print-header img{max-height:50px;max-width:150px;}.print-header h2{margin:0;color:${accentColor};font-size:1.5em;font-weight:bold;}table{width:100%;border-collapse:collapse;margin-bottom:1rem;page-break-inside:auto;}tr{page-break-inside:avoid;page-break-after:auto;}th,td{border:1px solid #ccc !important;padding:6px 8px;text-align:left;vertical-align:top;font-size:9pt;word-wrap:break-word;}th{background-color:${primaryColor};font-weight:bold;}h3,h4,h5{margin-top:1rem;margin-bottom:.5rem;color:${accentColor};font-weight:bold;}ul{padding-left:20px;margin-top:.2rem;margin-bottom:.2rem;}li{margin-bottom:.1rem;}button,select,input:not([type=checkbox]):not([type=radio]),.status-legend,.crud-buttons,.no-print,.alert-message,nav,aside,form{display:none !important;}.timetable th{text-align:center;}.timetable td{text-align:center;font-size:8pt;height:auto;min-height:30px;}</style></head><body onload="window.print(); setTimeout(window.close, 150);"><div class="print-header">${logoBase64 ? `<img src="${logoBase64}" alt="Logo">` : '<span></span>'}<h2>${title}</h2><span>Date: ${new Date().toLocaleDateString('fr-FR')}</span></div>${printContents}</body></html>`); popupWin.document.close(); }; reader.onerror = (e) => { console.error("Err logo base64:", e); printElementNoLogo(elementToPrint, title); }; if(blob) { reader.readAsDataURL(blob); } else { reader.onloadend(); } }).catch(err => { console.error("Err fetch logo:", err); printElementNoLogo(elementToPrint, title); }); } catch (e) { console.error("Erreur impression:", e); showAlert("Impossible de générer l'aperçu.", "error"); } }
    function printElementNoLogo(elementToPrint, title = "Impression") { const printContents = elementToPrint.outerHTML; const primaryColor = '#faf0d6'; const secondaryColor = '#ffc107'; const accentColor = '#198754'; const textDark = '#2e2e2e'; const bgColor = '#ffffff'; const popupWinNoLogo = window.open('', '_blank', 'top=0,left=0,height=100%,width=auto'); if(!popupWinNoLogo){ showAlert("Vérifiez bloqueurs de popup.", "error"); return;} popupWinNoLogo.document.open(); popupWinNoLogo.document.write(`<!DOCTYPE html><html lang="fr"><head><meta charset="UTF-8"><title>${title}</title><style>/* Copiez les styles d'impression ici aussi */ @media print{body{-webkit-print-color-adjust:exact !important;print-color-adjust:exact !important;background-color:${bgColor} !important;} .print-header{border-bottom-color:${secondaryColor} !important;} th{background-color:${primaryColor} !important;} td.available{background-color:#e0f2fe !important;}td.prep{background-color:#fef9c3 !important;}td.record{background-color:#dcfce7 !important;}td.unavailable{background-color:#fecaca !important;}.no-print{display:none !important;}} body{font-family:'Inter',sans-serif;padding:20px;font-size:10pt;line-height:1.4;color:${textDark};background-color:${bgColor};}.print-header{display:flex;justify-content:space-between;align-items:center;border-bottom:2px solid ${secondaryColor};padding-bottom:10px;margin-bottom:20px;}.print-header h2{margin:0;color:${accentColor};font-size:1.5em;font-weight:bold;}table{width:100%;border-collapse:collapse;margin-bottom:1rem;}th,td{border:1px solid #ccc !important;padding:6px 8px;text-align:left;vertical-align:top;font-size:9pt;}th{background-color:${primaryColor};font-weight:bold;}h3,h4,h5{margin-top:1rem;margin-bottom:.5rem;color:${accentColor};}.timetable th,.timetable td{text-align:center;}.no-print{display:none !important;}</style></head><body onload="window.print(); setTimeout(window.close, 150);"><div class="print-header"><span></span><h2>${title}</h2><span>Date: ${new Date().toLocaleDateString('fr-FR')}</span></div>${printContents}</body></html>`); popupWinNoLogo.document.close(); }

    // ==========================================================
    // === NAVIGATION ===
    // ==========================================================
    function setActiveSection(sectionId) { if (!sectionId) sectionId = 'accueil'; let sectionFound = false; if(navLinks) navLinks.forEach(link => { const linkSection = link.getAttribute('data-section'); link.classList.toggle('active', linkSection === sectionId); }); if(contentSections) contentSections.forEach(section => { const isActive = section.id === sectionId; section.classList.toggle('active', isActive); if(isActive) sectionFound = true; }); if (!sectionFound && sectionId !== 'accueil') { console.warn(`Section "${sectionId}" non trouvée, affichage accueil.`); setActiveSection('accueil'); if (window.location.hash !== '#accueil') window.location.hash = '#accueil'; return; } clearGlobalAlert(); if (sectionId === 'accueil' && document.getElementById('dashboard-content') && !document.getElementById('dashboard-content').querySelector('.stats-card')) { renderDashboard(); } if (sectionId === 'classes' && classesStructureContainer && (!currentStructureCache || !classesStructureContainer.querySelector('.crud-buttons'))) { renderClassStructure(); } if (sectionId === 'enseignants' && teacherMatieresListContainer && allMatieresCache.length === 0) { loadMatieresForForm(); } if (sectionId === 'horaires' && selectTeacherSchedule && selectTeacherSchedule.value === "") { if(teacherTimetableContainer) teacherTimetableContainer.innerHTML = '<p class="text-gray-500 italic text-center p-4">Sélectionnez un enseignant.</p>'; const printBtn = document.getElementById('print-timetable-btn'); const resetBtn = document.getElementById('reset-timetable-btn'); if(printBtn) printBtn.style.display='none'; if(resetBtn) resetBtn.style.display='none'; } else if (sectionId !== 'horaires' && teacherTimetableContainer && teacherTimetableContainer.innerHTML !== '' && !teacherTimetableContainer.innerHTML.includes('Sélectionnez')) { if(selectTeacherSchedule) selectTeacherSchedule.value = ''; teacherTimetableContainer.innerHTML = '<p class="text-gray-500 italic text-center p-4">Sélectionnez un enseignant.</p>'; currentTeacherSchedule={}; const printBtn = document.getElementById('print-timetable-btn'); const resetBtn = document.getElementById('reset-timetable-btn'); if(printBtn) printBtn.style.display='none'; if(resetBtn) resetBtn.style.display='none';} }
    if(navLinks) navLinks.forEach(link => { link.addEventListener('click', (event) => { event.preventDefault(); const sectionId = link.getAttribute('data-section'); if(sectionId){ setActiveSection(sectionId); window.location.hash = sectionId; } }); });

    // ==========================================================
    // === GESTION DES ENSEIGNANTS ===
    // ==========================================================
    function renderTeacherList() { if (!teacherList || !selectTeacherSchedule || !planTeacherSelect) { console.error("DOM manquant pour renderTeacherList"); return; } teacherList.innerHTML = '<tr><td colspan="5" class="text-center py-4 text-gray-500 italic">Chargement...</td></tr>'; selectTeacherSchedule.innerHTML = '<option value="">Chargement...</option>'; planTeacherSelect.innerHTML = '<option value="">Chargement...</option>'; fetch('/api/enseignants').then(handleApiResponse).then(data => { localTeacherCache = data; teacherList.innerHTML = ''; selectTeacherSchedule.innerHTML = '<option value="">-- Sélectionner --</option>'; planTeacherSelect.innerHTML = '<option value="">-- Sélectionner --</option>'; if (!data || data.length === 0) { teacherList.innerHTML = '<tr><td colspan="5" class="text-center py-4 text-gray-500">Aucun enseignant trouvé.</td></tr>'; } else { data.forEach(teacher => { const row = document.createElement('tr'); let matieresHtml = teacher.matieres && teacher.matieres.length > 0 ? teacher.matieres.map(m => escapeJsString(m.nom) + (m.classe_id ? ` (${escapeJsString(m.classe_id)})` : '')).join(', ') : '<span class="text-xs italic text-gray-400">Aucune</span>'; let commentairesHtml = teacher.commentaires ? escapeJsString(teacher.commentaires) : '<span class="text-xs italic text-gray-400">Aucun</span>'; row.innerHTML = `<td class="align-top">${teacher.nom || 'N/A'}</td><td class="align-top whitespace-normal break-words">${teacher.telephone || 'N/A'}</td><td class="text-xs align-top whitespace-normal break-words">${matieresHtml}</td><td class="text-xs align-top whitespace-normal break-words">${commentairesHtml}</td><td class="align-top whitespace-nowrap"><div class="flex flex-col space-y-1 md:flex-row md:space-y-0 md:space-x-1"><button class="text-xs p-1 bg-yellow-500 hover:bg-yellow-600 rounded text-white w-full md:w-auto" onclick="app.editTeacher(${teacher.id})">✏️ Modifier</button><button class="text-xs p-1 bg-red-600 hover:bg-red-700 rounded text-white w-full md:w-auto" onclick="app.deleteTeacher(${teacher.id})">🗑️ Suppr.</button></div></td>`; if(teacherList) teacherList.appendChild(row); if(selectTeacherSchedule) selectTeacherSchedule.add(new Option(teacher.nom || 'N/A', teacher.id)); if(planTeacherSelect) planTeacherSelect.add(new Option(teacher.nom || 'N/A', teacher.id)); }); } }).catch(error => { handleApiError(error); if(teacherList) teacherList.innerHTML = '<tr><td colspan="5" class="text-center py-4 text-red-500">Erreur chargement enseignants.</td></tr>'; if(selectTeacherSchedule) selectTeacherSchedule.innerHTML = '<option value="">Erreur</option>'; if(planTeacherSelect) planTeacherSelect.innerHTML = '<option value="">Erreur</option>'; }); }
    function resetTeacherForm() { if (!teacherForm) return; teacherForm.reset(); if(teacherIdInput) teacherIdInput.value = ''; if(teacherMatieresListContainer) { teacherMatieresListContainer.querySelectorAll('input[type="checkbox"]').forEach(cb => cb.checked = false); } if(teacherFormTitle) teacherFormTitle.textContent = 'Ajouter un Nouvel Enseignant'; if(saveTeacherBtn) { saveTeacherBtn.textContent = 'Enregistrer'; saveTeacherBtn.disabled = false; } if(cancelTeacherBtn) cancelTeacherBtn.style.display = 'none'; }
    function loadMatieresForForm() { if (!teacherMatieresListContainer) return; teacherMatieresListContainer.innerHTML = '<p class="text-sm text-gray-500 italic">Chargement...</p>'; if (allMatieresCache.length > 0) { populateMatieresCheckboxes(allMatieresCache); return; } fetch('/api/matieres').then(handleApiResponse).then(data => { allMatieresCache = data; populateMatieresCheckboxes(data); }).catch(error => { handleApiError(error); if(teacherMatieresListContainer) teacherMatieresListContainer.innerHTML = '<p class="text-red-500 text-sm">Erreur chargement matières.</p>'; }); }
    function populateMatieresCheckboxes(matieres) { if (!teacherMatieresListContainer) return; if (!matieres || matieres.length === 0) { teacherMatieresListContainer.innerHTML = '<p class="text-gray-500 italic text-sm">Aucune matière définie. Ajoutez-en via "Gestion Structure".</p>'; return; } let html = ''; matieres.sort((a, b) => a.nom.localeCompare(b.nom)).forEach(matiere => { html += `<div class="flex items-center"><input id="matiere-${matiere.id}" name="matiere_ids[]" type="checkbox" value="${matiere.id}" class="h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-500"><label for="matiere-${matiere.id}" class="ml-2 block text-sm text-gray-900">${escapeJsString(matiere.nom)} <span class="text-xs text-gray-500">(${matiere.classe?.nom || '?'})</span></label></div>`; }); teacherMatieresListContainer.innerHTML = html; }
    function editTeacher(id) { if (!teacherForm || !saveTeacherBtn || !teacherIdInput || !teacherFormTitle) return; clearGlobalAlert(); resetTeacherForm(); teacherFormTitle.textContent = 'Chargement Enseignant...'; saveTeacherBtn.disabled = true; if (teacherMatieresListContainer && allMatieresCache.length === 0) { loadMatieresForForm(); } fetch(`/api/enseignants/${id}`).then(handleApiResponse).then(teacher => { if (!teacher) { throw new Error("Enseignant non trouvé."); } teacherIdInput.value = teacher.id; document.getElementById('nom').value = teacher.nom || ''; document.getElementById('telephone').value = teacher.telephone || ''; document.getElementById('commentaires').value = teacher.commentaires || ''; setTimeout(() => { if (teacherMatieresListContainer) { teacherMatieresListContainer.querySelectorAll('input[type="checkbox"]').forEach(checkbox => { checkbox.checked = false; }); if (teacher.matieres && teacher.matieres.length > 0) { teacher.matieres.forEach(matiere => { const checkbox = teacherMatieresListContainer.querySelector(`input[value="${matiere.id}"]`); if (checkbox) checkbox.checked = true; }); } } }, 150); teacherFormTitle.textContent = 'Modifier l\'Enseignant'; saveTeacherBtn.textContent = 'Mettre à jour'; if(cancelTeacherBtn) cancelTeacherBtn.style.display = 'inline-block'; teacherForm.scrollIntoView({ behavior: 'smooth', block: 'start' }); }).catch(error => { handleApiError(error); resetTeacherForm(); }).finally(() => { if(saveTeacherBtn) saveTeacherBtn.disabled = false; }); }
    function teacherFormSubmitHandler(event) { if(!teacherForm || !saveTeacherBtn) return; event.preventDefault(); clearGlobalAlert(); const formData = new FormData(teacherForm); const teacherId = teacherIdInput.value; const isUpdating = !!teacherId; const selectedMatiereIds = []; if (teacherMatieresListContainer) { teacherMatieresListContainer.querySelectorAll('input[type="checkbox"]:checked').forEach(checkbox => { selectedMatiereIds.push(parseInt(checkbox.value)); }); } const teacherData = { nom: formData.get('nom'), telephone: formData.get('telephone') || null, commentaires: formData.get('commentaires') || null, matiere_ids: selectedMatiereIds }; let apiUrl = '/api/enseignants'; let apiMethod = 'POST'; if (isUpdating) { apiMethod = 'PUT'; apiUrl += `/${teacherId}`; } saveTeacherBtn.disabled = true; saveTeacherBtn.textContent = isUpdating ? 'Mise à jour...' : 'Enregistrement...'; fetch(apiUrl, { method: apiMethod, headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(teacherData) }).then(handleApiResponse).then(data => { showAlert(isUpdating ? 'Enseignant mis à jour.' : 'Enseignant créé.', 'success'); resetTeacherForm(); renderTeacherList(); }).catch(error => { handleApiError(error); }).finally(() => { saveTeacherBtn.disabled = false; if (!isUpdating) resetTeacherForm(); else saveTeacherBtn.textContent = 'Mettre à jour'; }); }
    if (teacherForm) { teacherForm.addEventListener('submit', teacherFormSubmitHandler); }
    if (cancelTeacherBtn) cancelTeacherBtn.addEventListener('click', resetTeacherForm);
    function deleteTeacher(id) { if (!id) return; clearGlobalAlert(); if (confirm(`Êtes-vous sûr de vouloir supprimer l'enseignant ID ${id} ?`)) { fetch(`/api/enseignants/${id}`, { method: 'DELETE', headers: {'Accept': 'application/json'} }).then(handleApiResponse).then(data => { showAlert(data.message || 'Enseignant supprimé.', 'success'); renderTeacherList(); if (selectTeacherSchedule && selectTeacherSchedule.value === String(id)) { selectTeacherSchedule.value = ""; if(teacherTimetableContainer) teacherTimetableContainer.innerHTML=''; } if (planTeacherSelect && planTeacherSelect.value === String(id)) { planTeacherSelect.value = ""; } }).catch(handleApiError); } }

    // ==========================================================
    // === EMPLOI DU TEMPS ENSEIGNANT (CONNECTÉ) ===
    // ==========================================================
    const days = ['Lun', 'Mar', 'Mer', 'Jeu', 'Ven']; const hours = Array.from({ length: 11 }, (_, i) => 8 + i);
    function renderTimetable(teacherId) { if (!teacherTimetableContainer || !teacherId) { console.error("Conteneur EDT ou ID enseignant manquant.", teacherId); return; } teacherTimetableContainer.innerHTML = '<p class="p-4 text-center italic text-gray-500">Chargement...</p>'; const printBtnElement = document.getElementById('print-timetable-btn'); const resetBtnElement = document.getElementById('reset-timetable-btn'); if (printBtnElement) printBtnElement.style.display = 'none'; if (resetBtnElement) resetBtnElement.style.display = 'none'; currentTeacherSchedule = {}; fetch(`/api/enseignants/${teacherId}/schedule`).then(handleApiResponse).then(scheduleData => { currentTeacherSchedule = scheduleData || {}; let html = '<table class="timetable w-full border-collapse"><thead><tr><th class="border p-1 text-xs font-semibold">Heure</th>'; days.forEach(day => html += `<th class="border p-1 text-xs font-semibold">${day}</th>`); html += '</tr></thead><tbody>'; hours.forEach(hour => { html += `<tr><td class="time-label border p-1 text-xs font-medium">${hour}:00-${hour+1}:00</td>`; days.forEach(day => { const slotId = `${day}-${String(hour).padStart(2,'0')}`; const slotStatus = currentTeacherSchedule[slotId]||'available'; html += `<td class="border p-0 ${slotStatus}" data-slot-id="${slotId}" onclick="app.toggleSlotStatus(this, ${teacherId})"></td>`; }); html+='</tr>';}); html += '</tbody></table>'; teacherTimetableContainer.innerHTML = html; if (printBtnElement) { printBtnElement.style.display = 'inline-block'; printBtnElement.onclick = () => { const teacherName = localTeacherCache.find(t => t.id == teacherId)?.nom || `Enseignant ID ${teacherId}`; printElement(teacherTimetableContainer.querySelector('table'), `Emploi du Temps - ${teacherName}`); }; } if (resetBtnElement) { resetBtnElement.style.display = 'inline-block'; resetBtnElement.onclick = () => resetTimetable(teacherId); } }).catch(error => { handleApiError(error); teacherTimetableContainer.innerHTML = '<p class="p-4 text-red-500 text-center italic">Erreur chargement.</p>'; if (printBtnElement) printBtnElement.style.display = 'none'; if (resetBtnElement) resetBtnElement.style.display = 'none'; }); }
    function toggleSlotStatus(cell, teacherId) { const slotId = cell.getAttribute('data-slot-id'); if (!slotId || !teacherId) return; const statusClasses = ['available', 'prep', 'record', 'unavailable']; let currentStatus = statusClasses.find(s => cell.classList.contains(s)) || 'available'; const nextStatusIndex = (statusClasses.indexOf(currentStatus) + 1) % statusClasses.length; const nextStatus = statusClasses[nextStatusIndex]; cell.classList.remove(...statusClasses); cell.classList.add(nextStatus); if (nextStatus === 'available') delete currentTeacherSchedule[slotId]; else currentTeacherSchedule[slotId] = nextStatus; clearTimeout(saveTimeout); let saveIndicator = document.getElementById('edt-save-indicator'); if(saveIndicator) saveIndicator.textContent = ' (Modifié...)'; saveTimeout = setTimeout(() => { saveTimetable(teacherId); }, 1500); }
    function saveTimetable(teacherId) { if (!teacherId) return; console.log(`Sauvegarde EDT pour ${teacherId}...`); let saveIndicator = document.getElementById('edt-save-indicator'); let titleElement = document.querySelector('#horaires h2'); if(!saveIndicator && titleElement) { try { saveIndicator = document.createElement('span'); saveIndicator.id = 'edt-save-indicator'; saveIndicator.className = 'text-sm text-gray-500 ml-2 font-normal'; titleElement.appendChild(saveIndicator); } catch(e){console.error("Impossible d'ajouter indicateur")} } if(saveIndicator) saveIndicator.textContent = ' (Sauvegarde...)'; fetch(`/api/enseignants/${teacherId}/schedule`, { method: 'PUT', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(currentTeacherSchedule) }).then(handleApiResponse).then(data => { console.log("Sauvegarde EDT réussie:", data.message); if(saveIndicator) saveIndicator.textContent = ' (Enregistré)'; setTimeout(()=> { if(saveIndicator) saveIndicator.textContent = ''; }, 2000); }).catch(error => { handleApiError(error); showAlert("Erreur sauvegarde EDT !", 'error'); if(saveIndicator) saveIndicator.textContent = ' (Erreur!)'; }); }
    function resetTimetable(teacherId) { if (!teacherId) { console.error("ID manquant"); return; } clearGlobalAlert(); if (confirm("Voulez-vous vraiment effacer l'emploi du temps de cet enseignant ?")) { currentTeacherSchedule = {}; saveTimetable(teacherId); renderTimetable(teacherId); showAlert("Emploi du temps réinitialisé.", "info"); } }
    function handleTeacherScheduleChange(event) { const id = parseInt(event.target.value); const container = document.getElementById('teacher-timetable-container'); const printBtn = document.getElementById('print-timetable-btn'); const resetBtn = document.getElementById('reset-timetable-btn'); if(id){ renderTimetable(id); } else { if(container) container.innerHTML = '<p class="text-gray-500 italic text-center p-4">Sélectionnez un enseignant.</p>'; currentTeacherSchedule = {}; if(printBtn) printBtn.style.display='none'; if(resetBtn) resetBtn.style.display='none'; } }
    if (selectTeacherSchedule) { selectTeacherSchedule.addEventListener('change', handleTeacherScheduleChange); }

    // ==========================================================
    // === GESTION STRUCTURE PÉDAGOGIQUE ===
    // ==========================================================
    function renderClassStructure() { if (!classesStructureContainer) return; classesStructureContainer.innerHTML = '<p class="p-4 italic text-gray-500">Chargement...</p>'; fetch('/api/structure').then(handleApiResponse).then(data => { currentStructureCache = data; classesStructureContainer.innerHTML = ''; classesStructureContainer.innerHTML += `<div class="mb-4"><button onclick="app.promptAddClass()" class="bg-green-600 hover:bg-green-700 text-white py-1 px-3 rounded text-sm">➕ Ajouter Classe</button></div>`; if (!data || data.length === 0) { classesStructureContainer.innerHTML += '<p class="text-gray-500 italic">Aucune classe.</p>'; return; } let html = '<div class="space-y-4">'; data.sort((a,b)=>a.nom.localeCompare(b.nom)).forEach(classe => { html += `<div class="p-4 border rounded-lg bg-gray-50 relative group" data-classe-id="${classe.id}"><div class="absolute top-2 right-2 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 flex space-x-1 z-10 crud-buttons"><button onclick="app.promptAddMatiere('${classe.id}')" class="p-1 bg-blue-500" title="Ajouter Matière">➕M</button><button onclick="app.promptEditClass('${classe.id}', '${escapeJsString(classe.nom)}')" class="p-1 bg-yellow-500" title="Modifier Classe">✏️</button><button onclick="app.confirmDeleteClass('${classe.id}', '${escapeJsString(classe.nom)}')" class="p-1 bg-red-600" title="Supprimer Classe">🗑️</button></div><h3 class="text-xl font-semibold mb-2">${classe.nom} <span class="text-sm text-gray-500">(${classe.id})</span></h3>`; if (classe.matieres && classe.matieres.length > 0) { html += '<ul class="ml-4 space-y-3">'; classe.matieres.sort((a,b)=>a.nom.localeCompare(b.nom)).forEach(matiere => { html += `<li class="relative group/matiere border-l-2 pl-3 border-indigo-200 py-1" data-matiere-id="${matiere.id}"><div class="absolute top-1 right-0 opacity-100 sm:opacity-0 sm:group-hover/matiere:opacity-100 flex space-x-1 z-10 crud-buttons"><button onclick="app.promptAddChapitre(${matiere.id})" class="p-1 bg-blue-400" title="Ajouter Chapitre">➕C</button><button onclick="app.promptEditMatiere(${matiere.id}, '${escapeJsString(matiere.nom)}')" class="p-1 bg-yellow-400 text-gray-800" title="Modifier Matière">✏️</button><button onclick="app.confirmDeleteMatiere(${matiere.id}, '${escapeJsString(matiere.nom)}')" class="p-1 bg-red-500" title="Supprimer Matière">🗑️</button></div><strong class="text-indigo-700">${matiere.nom}</strong>`; if (matiere.chapitres && matiere.chapitres.length > 0) { html += '<ul class="ml-6 list-disc space-y-2 mt-1">'; matiere.chapitres.sort((a,b)=>(a.ordre ?? 0) - (b.ordre ?? 0) || a.nom.localeCompare(b.nom)).forEach(chapitre => { html += `<li class="relative group/chapitre" data-chapitre-id="${chapitre.id}"><div class="absolute top-0 right-0 opacity-100 sm:opacity-0 sm:group-hover/chapitre:opacity-100 flex space-x-1 z-10 crud-buttons"><button onclick="app.promptAddLecon(${chapitre.id})" class="p-1 bg-blue-300 text-gray-800" title="Ajouter Leçon">➕L</button><button onclick="app.promptEditChapitre(${chapitre.id}, '${escapeJsString(chapitre.nom)}')" class="p-1 bg-yellow-300 text-gray-800" title="Modifier Chapitre">✏️</button><button onclick="app.confirmDeleteChapitre(${chapitre.id}, '${escapeJsString(chapitre.nom)}')" class="p-1 bg-red-400" title="Supprimer Chapitre">🗑️</button></div><span>${chapitre.nom}</span>`; if (chapitre.lecons && chapitre.lecons.length > 0) { html += '<ul class="ml-8 list-decimal text-sm space-y-1 mt-1">'; chapitre.lecons.sort((a,b)=> (a.num || '').localeCompare(b.num || '', undefined, {numeric: true, sensitivity: 'base'})).forEach(lecon => { html += `<li class="relative group/lecon" data-lecon-id="${lecon.id}"><div class="absolute top-0 right-0 opacity-100 sm:opacity-0 sm:group-hover/lecon:opacity-100 flex space-x-1 z-10 crud-buttons"><button onclick="app.promptEditLecon(${lecon.id}, '${escapeJsString(lecon.titre)}')" class="p-1 bg-yellow-200 text-gray-800" title="Modifier Leçon (via FdR)">✏️</button><button onclick="app.confirmDeleteLecon(${lecon.id}, '${escapeJsString(lecon.titre)}')" class="p-1 bg-red-300" title="Supprimer Leçon">🗑️</button></div><span><strong>${lecon.num || '?'}</strong>: ${lecon.titre || 'N/A'} <span class="text-gray-500 text-xs">(${lecon.responsable?.nom || 'N/A'})</span></span></li>`; }); html += '</ul>'; } else { html += '<p class="text-xs text-gray-400 italic ml-8">Aucune leçon.</p>'; } html += '</li>'; }); html += '</ul>'; } else { html += '<p class="text-sm italic ml-6">Aucun chapitre.</p>'; } html += '</li>'; }); html += '</ul>'; } else { html += '<p class="text-sm italic ml-4">Aucune matière.</p>'; } html += `</div>`; }); html += '</div>'; classesStructureContainer.innerHTML += html; document.querySelectorAll('.crud-buttons button').forEach(btn => { btn.classList.add('text-white', 'rounded', 'px-1.5', 'py-0.5', 'text-xs', 'shadow-sm'); }); }).catch(error => { handleApiError(error); classesStructureContainer.innerHTML = `<p class="p-4 text-red-500">Erreur chargement structure.</p>`; }); }
    function promptAddClass() { clearGlobalAlert(); const id=prompt("ID Classe (court, ex: tsexp):"); if(!id) return; const nom=prompt("Nom Classe:"); if(!nom) return; addClass(id.trim(), nom.trim()); }
    function addClass(id, nom) { fetch('/api/classes', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ id: id, nom: nom }) }).then(handleApiResponse).then(data => { showAlert('Classe ajoutée.', 'success'); renderClassStructure(); populateClasseSelect(); loadMatieresForForm(); }).catch(handleApiError); }
    function promptEditClass(id, currentNom) { clearGlobalAlert(); const nom=prompt(`Nouveau nom pour "${currentNom}":`, currentNom); if(nom && nom.trim() !== currentNom) editClass(id, nom.trim()); }
    function editClass(id, nom) { fetch(`/api/classes/${id}`, { method: 'PUT', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ nom: nom }) }).then(handleApiResponse).then(data => { showAlert('Classe màj.', 'success'); renderClassStructure(); populateClasseSelect(); loadMatieresForForm(); }).catch(handleApiError); }
    function confirmDeleteClass(id, nom) { clearGlobalAlert(); if(confirm(`Supprimer classe "${nom}" (${id}) et tout son contenu?`)) deleteClass(id); }
    function deleteClass(id) { fetch(`/api/classes/${id}`, { method: 'DELETE', headers: {'Accept': 'application/json'} }).then(handleApiResponse).then(data => { showAlert(data.message || 'Classe supprimée.', 'success'); renderClassStructure(); populateClasseSelect(); loadMatieresForForm(); }).catch(handleApiError); }
    function promptAddMatiere(classeId) { clearGlobalAlert(); const nom = prompt(`Nom nouvelle matière pour classe ${classeId}:`); if (nom && nom.trim()) addMatiere(classeId, nom.trim()); }
    function addMatiere(classeId, nom) { fetch('/api/matieres', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ classe_id: classeId, nom: nom }) }).then(handleApiResponse).then(data => { showAlert('Matière ajoutée.', 'success'); renderClassStructure(); loadMatieresForForm(); }).catch(handleApiError); }
    function promptEditMatiere(matiereId, currentNom) { clearGlobalAlert(); const nom = prompt(`Nouveau nom pour "${currentNom}":`, currentNom); if (nom && nom.trim() !== currentNom) editMatiere(matiereId, nom.trim()); }
    function editMatiere(id, nom) { fetch(`/api/matieres/${id}`, { method: 'PUT', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ nom: nom }) }).then(handleApiResponse).then(data => { showAlert('Matière màj.', 'success'); renderClassStructure(); loadMatieresForForm(); }).catch(handleApiError); }
    function confirmDeleteMatiere(id, nom) { clearGlobalAlert(); if (confirm(`Supprimer matière "${nom}" (${id}) et ses chapitres/leçons?`)) deleteMatiere(id); }
    function deleteMatiere(id) { fetch(`/api/matieres/${id}`, { method: 'DELETE', headers: {'Accept': 'application/json'} }).then(handleApiResponse).then(data => { showAlert(data.message || 'Matière supprimée.', 'success'); renderClassStructure(); loadMatieresForForm(); }).catch(handleApiError); }
    function promptAddChapitre(matiereId) { clearGlobalAlert(); const nom = prompt(`Nom nouveau chapitre pour matière ID ${matiereId}:`); if (nom && nom.trim()) addChapitre(matiereId, nom.trim()); }
    function addChapitre(matiereId, nom) { fetch('/api/chapitres', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ matiere_id: matiereId, nom: nom }) }).then(handleApiResponse).then(data => { showAlert('Chapitre ajouté.', 'success'); renderClassStructure(); }).catch(handleApiError); }
    function promptEditChapitre(chapitreId, currentNom) { clearGlobalAlert(); const nom = prompt(`Nouveau nom pour "${currentNom}":`, currentNom); if (nom && nom.trim() !== currentNom) editChapitre(chapitreId, nom.trim()); }
    function editChapitre(id, nom) { fetch(`/api/chapitres/${id}`, { method: 'PUT', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ nom: nom }) }).then(handleApiResponse).then(data => { showAlert('Chapitre màj.', 'success'); renderClassStructure(); }).catch(handleApiError); }
    function confirmDeleteChapitre(id, nom) { clearGlobalAlert(); if (confirm(`Supprimer chapitre "${nom}" (${id}) et ses leçons?`)) deleteChapitre(id); }
    function deleteChapitre(id) { fetch(`/api/chapitres/${id}`, { method: 'DELETE', headers: {'Accept': 'application/json'} }).then(handleApiResponse).then(data => { showAlert(data.message || 'Chapitre supprimé.', 'success'); renderClassStructure(); }).catch(handleApiError); }
    function promptAddLecon(chapitreId) { clearGlobalAlert(); const num = prompt(`N° leçon pour chap. ID ${chapitreId}:`); if (!num) return; const titre = prompt(`Titre leçon (N° ${num}):`); if (titre && titre.trim()) addLecon(chapitreId, num.trim(), titre.trim()); }
    function addLecon(chapitreId, num, titre) { fetch('/api/lecons', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ chapitre_id: chapitreId, num: num, titre: titre }) }).then(handleApiResponse).then(data => { showAlert('Leçon ajoutée.', 'success'); renderClassStructure(); }).catch(handleApiError); }
    function promptEditLecon(leconId, currentTitre) { showAlert("L'édition détaillée des leçons se fait via la Feuille de Route.", 'info'); }
    function editLecon(id, titre) { showAlert("Fonction non implémentée ici.", "warning"); }
    function confirmDeleteLecon(id, titre) { clearGlobalAlert(); if (confirm(`Supprimer leçon "${titre}" (${id})?`)) deleteLecon(id); }
    function deleteLecon(id) { fetch(`/api/lecons/${id}`, { method: 'DELETE', headers: {'Accept': 'application/json'} }).then(handleApiResponse).then(data => { showAlert(data.message || 'Leçon supprimée.', 'success'); renderClassStructure(); }).catch(handleApiError); }

    // === PLANIFICATION STUDIO ===
    function renderStudioPlanning(dateString) { if (!studioPlanningView) return; const dateRegex = /^\d{4}-\d{2}-\d{2}$/; if (!dateString || !dateRegex.test(dateString)) { studioPlanningView.innerHTML = '<p class="text-sm italic text-gray-500 p-4">Format date invalide.</p>'; if(dateString) console.error("Format date invalide:", dateString); return; } studioPlanningView.innerHTML = '<p class="text-sm italic p-4">Chargement...</p>'; const formattedDate = dateString; console.log(`Appel API Planning pour date: ${formattedDate}`); fetch(`/api/planning?date=${formattedDate}`).then(handleApiResponse).then(bookings => { studioPlanningView.innerHTML = ''; if (!bookings || bookings.length === 0) { studioPlanningView.innerHTML = `<p class="text-sm italic p-4">Aucune réservation pour ${formattedDate}.</p>`; return; } const planningByStudio = {}; bookings.forEach(b => { if(!planningByStudio[b.studio_id]) planningByStudio[b.studio_id] = []; planningByStudio[b.studio_id].push(b); }); Object.keys(planningByStudio).sort().forEach(studioId => { let html = `<h4 class="font-semibold mt-3">Studio ${studioId}</h4>`; const studioBookings = planningByStudio[studioId]; if (studioBookings.length > 0) { html += '<ul class="list-disc ml-5 text-sm">'; studioBookings.sort((a,b)=>a.heure_debut.localeCompare(b.heure_debut)).forEach(b => { const teacher = b.enseignant?.nom || 'Inconnu'; const start = b.heure_debut.substring(0,5); const end = b.heure_fin.substring(0,5); html += `<li>${start} - ${end}: ${teacher} (${escapeJsString(b.lecon_description||'N/A')}) <button class="text-xs text-red-600 hover:text-red-800 ml-1" onclick="app.cancelBookingApi(${b.id}, '${b.date_reservation}')">(Annuler)</button></li>`; }); html += '</ul>'; } else { html += `<p class="text-sm italic ml-2">Aucune réservation.</p>`; } studioPlanningView.innerHTML += html; }); }).catch(error => { handleApiError(error); studioPlanningView.innerHTML = `<p class="text-red-500 p-4 text-sm">Erreur chargement planning.</p>`; }); }
    function loadPlannableLessons(teacherId) { if (!planLessonSelect || !planLessonDetailsDiv) { console.error("Elements Select/Details leçon non trouvés."); return; } planLessonSelect.innerHTML = '<option value="">Chargement...</option>'; planLessonSelect.disabled = true; planLessonDetailsDiv.innerHTML = ''; if (!teacherId || teacherId === "") { planLessonSelect.innerHTML = '<option value="">-- Sélectionnez enseignant --</option>'; return; } fetch(`/api/enseignants/${teacherId}/lecons-planifiables`).then(handleApiResponse).then(lecons => { console.log(`Leçons planifiables reçues pour enseignant ${teacherId}:`, lecons); if (!lecons || lecons.length === 0) { planLessonSelect.innerHTML = '<option value="">-- Aucune leçon à planifier --</option>'; } else { planLessonSelect.innerHTML = '<option value="">-- Sélectionnez une leçon --</option>'; lecons.forEach(lecon => { let classeNom = lecon.chapitre?.matiere?.classe?.nom || '?'; let matiereNom = lecon.chapitre?.matiere?.nom || '?'; let chapitreNom = lecon.chapitre?.nom || '?'; let optionText = `${classeNom}>${matiereNom}>${chapitreNom}>${lecon.num || '?'} ${lecon.titre || 'Sans titre'}`; planLessonSelect.add(new Option(optionText, lecon.id)); }); } planLessonSelect.disabled = false; }).catch(error => { handleApiError(error); planLessonSelect.innerHTML = '<option value="">Erreur chargement</option>'; planLessonSelect.disabled = false; }); }
    if (planTeacherSelect) { planTeacherSelect.addEventListener('change', (event) => { loadPlannableLessons(event.target.value); }); }
    if(planLessonSelect) { planLessonSelect.addEventListener('change', (event) => { const lessonId = event.target.value; if(planLessonDetailsDiv) planLessonDetailsDiv.innerHTML = ''; if(planLessonDescInput) planLessonDescInput.value = ''; if (lessonId && planLessonSelect.selectedIndex > 0) { if(planLessonDescInput) { planLessonDescInput.value = planLessonSelect.options[planLessonSelect.selectedIndex].text; planLessonDescInput.readOnly = true; } } else { if(planLessonDescInput) { planLessonDescInput.placeholder="Ou entrez une description si leçon non listée"; planLessonDescInput.readOnly = false; } } }); }
    function planningFormSubmitHandler(event) { if(!planningForm || !planSubmitButton) return; event.preventDefault(); clearGlobalAlert(); const formData = new FormData(planningForm); const selectedLeconId = parseInt(formData.get('lecon_id')); let leconDescription = formData.get('lecon_description'); if (selectedLeconId && !leconDescription && planLessonSelect && planLessonSelect.selectedIndex > 0) { leconDescription = planLessonSelect.options[planLessonSelect.selectedIndex].text; } if (!selectedLeconId) { showAlert('Veuillez sélectionner une leçon.', 'error'); return; } const bookingData = { enseignant_id: parseInt(formData.get('enseignant_id')), lecon_id: selectedLeconId, lecon_description: leconDescription || "Leçon ID " + selectedLeconId, date_reservation: formData.get('date_reservation'), heure_debut: formData.get('heure_debut'), heure_fin: formData.get('heure_fin'), studio_id: parseInt(formData.get('studio_id')) }; if (bookingData.heure_fin <= bookingData.heure_debut) { showAlert('Fin avant début!', 'error'); return; } if (!bookingData.enseignant_id || !bookingData.date_reservation || !bookingData.heure_debut || !bookingData.heure_fin || !bookingData.studio_id || !bookingData.lecon_id ) { showAlert('Champs requis manquants.', 'error'); return; } planSubmitButton.disabled = true; planSubmitButton.textContent = 'Planification...'; fetch('/api/planning', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(bookingData) }).then(handleApiResponse).then(data => { showAlert(data.message || 'Session planifiée!', 'success'); planningForm.reset(); if (planLessonSelect) { planLessonSelect.innerHTML = '<option value="">-- Sélectionnez enseignant --</option>'; planLessonSelect.disabled = true; } if(planLessonDetailsDiv) planLessonDetailsDiv.innerHTML = ''; if(planLessonDescInput) { planLessonDescInput.value=''; planLessonDescInput.readOnly = false;} if (viewPlanningDateInput.value === bookingData.date_reservation) renderStudioPlanning(bookingData.date_reservation); }).catch(error => { handleApiError(error); }).finally(() => { planSubmitButton.disabled = false; planSubmitButton.textContent = 'Vérifier & Planifier'; }); }
    if(planningForm) { planningForm.addEventListener('submit', planningFormSubmitHandler); }
    function cancelBookingApi(bookingId, dateOfBooking) { if (!bookingId) return; clearGlobalAlert(); if (confirm(`Annuler réservation ID ${bookingId} ?`)) { fetch(`/api/planning/${bookingId}`, { method: 'DELETE', headers: {'Accept': 'application/json'} }).then(handleApiResponse).then(data => { showAlert(data.message || 'Réservation annulée.', 'success'); renderStudioPlanning(dateOfBooking); }).catch(handleApiError); } }
    if (viewPlanningDateInput) { viewPlanningDateInput.addEventListener('change', (event) => { renderStudioPlanning(event.target.value); }); }

    // === FEUILLE DE ROUTE ===
    function populateClasseSelect() { if(!selectClasseRoute) return; selectClasseRoute.innerHTML = '<option value="">Chargement...</option>'; fetch('/api/structure').then(handleApiResponse).then(data => { selectClasseRoute.innerHTML = '<option value="">-- Sélectionner --</option>'; if (data && Array.isArray(data) && data.length > 0) { data.sort((a,b)=>a.nom.localeCompare(b.nom)).forEach(classe => { selectClasseRoute.add(new Option(classe.nom, classe.id)); }); } else { selectClasseRoute.innerHTML = '<option value="">Aucune classe</option>'; } }).catch(error => { handleApiError(error); selectClasseRoute.innerHTML = '<option value="">Erreur</option>'; }); }
    function generateFeuilleRouteHTML(classeId, isBlank = false, isInteractive = false) { if(!routeContent || !saveChangesBtn) return; routeContent.innerHTML = '<p class="p-4 text-center text-gray-500 italic">Chargement...</p>'; saveChangesBtn.style.display = 'none'; fetch(`/api/structure?classe_id=${classeId}`).then(handleApiResponse).then(classe => { if (!classe || typeof classe !== 'object') throw new Error("Structure invalide."); let html = `<div class="p-4 print-container"><h3 class="text-xl font-semibold mb-4">Feuille de Route - ${classe.nom || 'Classe inconnue'}</h3>`; if (!isBlank && isInteractive) { html += `<div class="mb-4 status-legend text-xs no-print">Légende: 🔜À enregistrer 🔄En montage 🟡À revoir ✅Validée ❌À refaire</div>`; } if (!classe.matieres || classe.matieres.length === 0) { html += `<p class="p-4 text-center text-gray-500 italic">Aucune matière.</p>`; } else { classe.matieres.sort((a,b)=>a.nom.localeCompare(b.nom)).forEach(matiere => { html += `<h4 class="text-lg font-medium mt-4 mb-2 text-blue-700">${matiere.nom}</h4>`; if (matiere.chapitres && matiere.chapitres.length > 0) { matiere.chapitres.sort((a,b)=>(a.ordre??0) - (b.ordre??0) || a.nom.localeCompare(b.nom)).forEach(chapitre => { html += `<h5 class="text-md font-medium mt-3 mb-1 ml-4">${chapitre.nom}</h5>`; html += `<div class="overflow-x-auto border rounded-lg ml-4 mb-4"><table class="min-w-full text-sm"><thead><tr><th>N°</th><th>Titre</th><th>Statut</th><th>Date</th><th>Durée</th><th>Resp.</th><th>Comms</th></tr></thead><tbody>`; if (chapitre.lecons && chapitre.lecons.length > 0) { chapitre.lecons.sort((a,b)=> (a.num || '').localeCompare(b.num || '', undefined, {numeric: true, sensitivity: 'base'})).forEach(lecon => { const teacherName = lecon.responsable?.nom || 'N/A'; html += `<tr data-lesson-id="${lecon.id}"><td>${lecon.num||''}</td><td>${escapeJsString(lecon.titre||'')}</td>`; if (isInteractive && !isBlank) { html += `<td><select class="feuille-route-select" name="statut_${lecon.id}">`; statusOptions.forEach(opt => { html += `<option value="${opt.value}" ${lecon.statut === opt.value ? 'selected' : ''}>${opt.text}</option>`; }); html += `</select></td>`; html += `<td><input type="date" class="feuille-route-input" name="date_${lecon.id}" value="${lecon.date_enregistrement || ''}"></td>`; html += `<td><input type="number" class="feuille-route-input w-16" name="duree_${lecon.id}" value="${lecon.duree_min || ''}" min="0"></td>`; html += `<td>${teacherName}</td>`; html += `<td><input type="text" class="feuille-route-input feuille-route-comment" name="commentaires_${lecon.id}" value="${escapeJsString(lecon.commentaires || '')}"></td>`; } else { const statusText = statusOptions.find(opt => opt.value === lecon.statut)?.text || ''; html += `<td>${isBlank?'':statusText}</td><td>${isBlank?'':(lecon.date_enregistrement||'—')}</td><td>${isBlank?'':(lecon.duree_min||'')}</td><td>${isBlank?'':teacherName}</td><td>${isBlank?'':escapeJsString(lecon.commentaires||'')}</td>`; } html += `</tr>`; }); } else { html += `<tr><td colspan="7" class="text-center text-gray-500 py-2">Aucune leçon.</td></tr>`; } html += `</tbody></table></div>`; }); } else { html += `<p class="ml-4 italic text-gray-500 text-sm">Aucun chapitre.</p>`; } }); } html += `</div>`; routeContent.innerHTML = html; if (isInteractive && !isBlank) saveChangesBtn.style.display = 'inline-block'; }).catch(error => { handleApiError(error); routeContent.innerHTML = `<p class="p-4 text-red-500 text-center italic">Impossible de charger la feuille de route.</p>`; }); }
    if (generateRouteBtn) { generateRouteBtn.addEventListener('click', () => { const id = selectClasseRoute.value; clearGlobalAlert(); if(id) generateFeuilleRouteHTML(id, false, true); else { if(routeContent) routeContent.innerHTML='<p class="p-4 text-center text-gray-500">Sélectionnez une classe.</p>'; if(saveChangesBtn) saveChangesBtn.style.display='none'; } }); }
    if (generateBlankBtn) { generateBlankBtn.addEventListener('click', () => { const id = selectClasseRoute.value; clearGlobalAlert(); if(id) generateFeuilleRouteHTML(id, true, false); else { if(routeContent) routeContent.innerHTML='<p class="p-4 text-center text-gray-500">Sélectionnez une classe.</p>'; if(saveChangesBtn) saveChangesBtn.style.display='none'; } }); }
    if (saveChangesBtn) { saveChangesBtn.addEventListener('click', () => { clearGlobalAlert(); const rows = routeContent.querySelectorAll('tbody tr[data-lesson-id]'); const updatedLessonsData = []; rows.forEach(row => { const lessonId = row.getAttribute('data-lesson-id'); if (!lessonId || isNaN(parseInt(lessonId))) return; const statutInput = row.querySelector(`select[name="statut_${lessonId}"]`); const dateInput = row.querySelector(`input[name="date_${lessonId}"]`); const dureeInput = row.querySelector(`input[name="duree_${lessonId}"]`); const commentairesInput = row.querySelector(`input[name="commentaires_${lessonId}"]`); if (statutInput && dateInput && dureeInput && commentairesInput) { updatedLessonsData.push({ id: parseInt(lessonId), statut: statutInput.value, date_enregistrement: dateInput.value || null, duree_min: dureeInput.value !== '' ? parseInt(dureeInput.value) : null, commentaires: commentairesInput.value }); }}); if (updatedLessonsData.length === 0) { showAlert('Aucune donnée modifiée détectée.', 'info'); return; } saveChangesBtn.disabled = true; saveChangesBtn.textContent = 'Sauvegarde...'; fetch('/api/lecons-bulk-update', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(updatedLessonsData) }).then(handleApiResponse).then(data => { showAlert(data.message || 'Modifications enregistrées.', 'success'); }).catch(handleApiError).finally(() => { saveChangesBtn.disabled = false; saveChangesBtn.textContent = 'Enregistrer Modifications'; }); }); }
    if (printFdrBtn) { printFdrBtn.addEventListener('click', () => { printElement(routeContent.querySelector('.print-container') || routeContent, "Feuille de Route"); }); }

    // === TABLEAU DE BORD (ACCUEIL) ===
    function renderDashboard() { const dashboardContent = document.getElementById('dashboard-content'); const loadingIndicator = document.getElementById('dashboard-loading'); const statsContainer = document.getElementById('dashboard-stats-container'); const leconsContainer = document.getElementById('dashboard-lecons-container'); const planningContainer = document.getElementById('dashboard-planning-container'); if (!dashboardContent || !loadingIndicator || !statsContainer || !leconsContainer || !planningContainer) { console.error("Éléments du tableau de bord non trouvés."); return; } loadingIndicator.style.display = 'block'; statsContainer.style.display = 'none'; leconsContainer.style.display = 'none'; planningContainer.style.display = 'none'; fetch('/api/dashboard').then(handleApiResponse).then(data => { loadingIndicator.style.display = 'none'; statsContainer.style.display = 'block'; leconsContainer.style.display = 'block'; planningContainer.style.display = 'block'; if (data.stats) { document.getElementById('db-stat-classes').textContent = data.stats.totalClasses ?? '0'; document.getElementById('db-stat-matieres').textContent = data.stats.totalMatieres ?? '0'; document.getElementById('db-stat-chapitres').textContent = data.stats.totalChapitres ?? '0'; document.getElementById('db-stat-lecons').textContent = data.stats.totalLecons ?? '0'; const leconStatusDiv = document.getElementById('db-lecon-status'); if (leconStatusDiv) { leconStatusDiv.innerHTML = ''; const statusCounts = data.stats.leconStatusCounts || {}; const orderedStatus = ['torecord', 'editing', 'review', 'redo', 'validated']; let hasStatus = false; orderedStatus.forEach(statusKey => { const count = statusCounts[statusKey] || 0; const statusInfo = statusOptions.find(opt => opt.value === statusKey); if (statusInfo) { if(count > 0) hasStatus = true; let bgColor = 'bg-gray-200'; let textColor = 'text-gray-800'; if (statusKey === 'torecord') { bgColor = 'bg-blue-100'; textColor = 'text-blue-800';} else if (statusKey === 'editing') { bgColor = 'bg-purple-100'; textColor = 'text-purple-800';} else if (statusKey === 'review') { bgColor = 'bg-yellow-100'; textColor = 'text-yellow-800';} else if (statusKey === 'redo') { bgColor = 'bg-red-100'; textColor = 'text-red-800';} else if (statusKey === 'validated') { bgColor = 'bg-green-100'; textColor = 'text-green-800';} leconStatusDiv.innerHTML += `<span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium ${bgColor} ${textColor}">${statusInfo.text.split(' ')[0]} ${statusInfo.text.split(' ')[1]} : ${count}</span>`; } }); if (!hasStatus) { leconStatusDiv.innerHTML = '<p class="italic text-sm">Aucune leçon.</p>'; } } } else { document.getElementById('db-stat-classes').textContent = '?'; /* ... */ const leconStatusDiv = document.getElementById('db-lecon-status'); if(leconStatusDiv) leconStatusDiv.innerHTML = '<p italic text-sm">N/A</p>'; } const upcomingDiv = document.getElementById('db-upcoming-planning'); if (upcomingDiv) { upcomingDiv.innerHTML = ''; const bookings = data.upcomingBookings || []; if (bookings.length === 0) { upcomingDiv.innerHTML = '<p class="italic text-sm">Aucune réservation à venir.</p>'; } else { let listHtml = '<ul class="space-y-1">'; bookings.forEach(b => { const date = new Date(b.date_reservation + 'T00:00:00').toLocaleDateString('fr-FR', { weekday: 'short', day: 'numeric', month: 'short' }); const start = b.heure_debut.substring(0,5); const end = b.heure_fin.substring(0,5); const teacher = b.enseignant?.nom || 'Inconnu'; listHtml += `<li class="border-b pb-1">${date} (${start}-${end}) - St.${b.studio_id}: ${teacher} (${escapeJsString(b.lecon_description||'N/A')})</li>`; }); listHtml += '</ul>'; upcomingDiv.innerHTML = listHtml; } } }).catch(error => { handleApiError(error); loadingIndicator.style.display = 'none'; if(dashboardContent) dashboardContent.innerHTML = `<p class="text-red-500 p-4">Erreur chargement TdB.</p>`; }); }

    // === INITIALISATION ===
    function initializeApp() {
        console.log("Initialisation de l'application...");
        const initialSectionHash = window.location.hash.substring(1) || 'accueil';
        renderTeacherList();
        populateClasseSelect();
        loadMatieresForForm();
        if(viewPlanningDateInput) { const today = new Date().toISOString().split('T')[0]; if (!viewPlanningDateInput.value) viewPlanningDateInput.value = today; /* renderStudioPlanning appelé par setActiveSection */ }
        setActiveSection(initialSectionHash); // Appel final qui déclenche l'affichage de la section et son chargement de données si nécessaire
    }
    initializeApp();

    // === Exports pour onclick HTML ===
    window.app = { editTeacher, deleteTeacher, toggleSlotStatus, saveTimetable, resetTimetable, promptAddClass, addClass, promptEditClass, editClass, confirmDeleteClass, deleteClass, promptAddMatiere, addMatiere, promptEditMatiere, editMatiere, confirmDeleteMatiere, deleteMatiere, promptAddChapitre, addChapitre, promptEditChapitre, editChapitre, confirmDeleteChapitre, deleteChapitre, promptAddLecon, addLecon, promptEditLecon, editLecon, confirmDeleteLecon, deleteLecon, cancelBookingApi };

}); // Fin DOMContentLoaded Listener - Assurez-vous que c'est bien la dernière ligne et qu'il n'y a rien après.